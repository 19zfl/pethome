# 端口
server:
  port: 80
# 配置数据库连接信息
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:///pethome
    username: root
    password: root
# 日志打印
logging:
  level:
    com:
      zfl19:debug
